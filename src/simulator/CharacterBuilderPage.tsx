import { TeamSimulatorPage } from "./TeamSimulatorPage";

export function CharacterBuilderPage() {
  return (
    <TeamSimulatorPage
      title="角色构筑"
      description="检查单名角色的星魂、光锥、叠影与遗器效果，并保留完整版本证据。"
      maxSlots={1}
    />
  );
}
